import { NsAutoHeightTable } from './ns-auto-height-table';

describe('NsAutoHeightTable', () => {
  it('should create an instance', () => {
    const directive = new NsAutoHeightTable();
    expect(directive).toBeTruthy();
  });
});
